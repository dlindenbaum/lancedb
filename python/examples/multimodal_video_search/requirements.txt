# Core dependencies
lancedb>=0.22.0
pyarrow>=14.0.0

# ONNX Runtime for efficient inference
onnxruntime-gpu>=1.16.0  # Use onnxruntime>=1.16.0 for CPU-only
# For CPU only: pip install onnxruntime>=1.16.0

# Model utilities
transformers>=4.35.0  # For CLIP tokenizer
huggingface-hub>=0.19.0  # For model downloads

# Image processing
pillow>=10.0.0

# Video processing
opencv-python>=4.8.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0

# Optional for visualization
matplotlib>=3.7.0

# Face detection and recognition (optional)
deepface>=0.0.79
tf-keras>=2.15.0  # Required by deepface
# For GPU: tensorflow[and-cuda]
# For CPU only: pip install tensorflow

# People tracking (optional)
supervision>=0.16.0  # For YOLO tracking with ByteTrack
# Note: YOLO models are automatically downloaded from HuggingFace

# Person clustering (optional)
scikit-learn>=1.3.0  # For clustering algorithms (DBSCAN, Agglomerative)
